import { USER_DATE, ACCESS_TOKEN, EMAIL } from "../configuration";
import * as action from "../actions/action.type";

export const SignIn = () => {
  return (dispatch) => {};
};

export const SignOut = () => {
  return (dispatch) => {};
};
